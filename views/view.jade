include ./layout.jade
  .container-fluid
     #logo
     each item in torender     
        div(id= item.divId)!= item.html
          if isAuth 
            a(href="/" + item.name + "/edit")= "Edit this Page"
